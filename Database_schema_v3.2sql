-- Drop Tables (in correct order due to foreign keys)
DROP TABLE IF EXISTS work_history;
DROP TABLE IF EXISTS unit;
DROP TABLE IF EXISTS technician;

-- 1. Create Technician table with ID
CREATE TABLE technician (
    tech_id INT AUTO_INCREMENT PRIMARY KEY, 
    technician_name VARCHAR(50) UNIQUE NOT NULL,
    kickbacks INT DEFAULT 0,
    part_addons INT DEFAULT 0,
    misdiags INT DEFAULT 0
);

-- 2. Create Unit table
CREATE TABLE unit (
    service_tag VARCHAR(50) NOT NULL,
    job_number INT NOT NULL,
    current_status ENUM(
        'DIAGNOSED', 
        'REPAIRED', 
        'PARTIAL', 
        'BER', 
        'IMAGING COMPLETE',
        'COMPLETED QA',
        'COMPLETED BUGS',
        'COMPLETED BER'
    ) NOT NULL,
    on_hold_status ENUM(
        'AWAITING QA',
        'AWAITING ADP REPAIRS',
        'AWAITING WORTH REPAIRS',
        'AWAITING WARRANTY / NON-ADP REPAIRS',
        'AWAITING CHS REPAIRS',
        'BER',
        'BUG UNIT',
        'AWAITING REIMAGING',
        'AWAITING DELL DEPOT',
        'AWAITING OOS PARTS',
        'DOA PART'
    ),
    customer_name ENUM('RICHLAND 1', 'ANDERSON 5', 'FT. MILL', 'UNION', 'IREDELL'),
    assigned_technician VARCHAR(50),
    current_tech_id INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    PRIMARY KEY (service_tag, job_number),
    INDEX idx_status (current_status),
    INDEX idx_customer (customer_name),
    INDEX idx_technician (assigned_technician),
    INDEX idx_current_tech_id (current_tech_id),
    INDEX idx_updated (updated_at),
    CONSTRAINT fk_unit_tech FOREIGN KEY (current_tech_id) REFERENCES technician(tech_id) ON DELETE SET NULL ON UPDATE CASCADE
);

-- 3. Create Work History table (no foreign key constraint to avoid partition issues)
CREATE TABLE work_history (
    history_id INT AUTO_INCREMENT PRIMARY KEY,
    service_tag VARCHAR(50) NOT NULL,
    job_number INT,
    technician_name VARCHAR(50) NOT NULL,
    work_date DATE NOT NULL,
    status_before VARCHAR(50),
    status_after VARCHAR(50) NOT NULL,
    on_hold_status ENUM(
        'AWAITING QA',
        'AWAITING ADP REPAIRS',
        'AWAITING WORTH REPAIRS',
        'AWAITING WARRANTY / NON-ADP REPAIRS',
        'AWAITING CHS REPAIRS',
        'BER',
        'BUG UNIT',
        'AWAITING REIMAGING',
        'AWAITING DELL DEPOT',
        'AWAITING OOS PARTS',
        'DOA PART'
    ),
    notes TEXT,
    submitted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    INDEX idx_service_tag (service_tag),
    INDEX idx_technician (technician_name),
    INDEX idx_work_date (work_date),
    INDEX idx_submitted (submitted_at)
);

-- 4. Insert technician data
INSERT INTO technician (technician_name) VALUES 
('ANDRE'),('ANTHONY'),('ASHE'),('BRANDON'),('CODY'),('CHRISTIAN'),
('CRUZ'),('DARIUS'),('DAVID'),('DEMETRUIS'),('GEOFF'),('JESS'),
('JOSH'),('KYLE'),('PAM'),('RYAN'),('TRISTIN'),('TRINITY'),
('TURNER'),('TYREK'),('ZAC');

-- 5. Insert comprehensive sample units to test all statuses and on-hold statuses
-- DIAGNOSED units
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('FFH8VW3', 82071, 'DIAGNOSED', 'AWAITING WARRANTY / NON-ADP REPAIRS', 'RICHLAND 1', 'GEOFF', (SELECT tech_id FROM technician WHERE technician_name = 'GEOFF'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('ABC1234', 10001, 'DIAGNOSED', 'AWAITING QA', 'ANDERSON 5', 'BRANDON', (SELECT tech_id FROM technician WHERE technician_name = 'BRANDON'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('DEF5678', 10002, 'DIAGNOSED', 'AWAITING ADP REPAIRS', 'FT. MILL', 'JOSH', (SELECT tech_id FROM technician WHERE technician_name = 'JOSH'));

-- REPAIRED units
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('a1b2c3d', 12345, 'REPAIRED', 'AWAITING QA', 'ANDERSON 5', 'BRANDON', (SELECT tech_id FROM technician WHERE technician_name = 'BRANDON'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('GHI9012', 10003, 'REPAIRED', 'AWAITING WORTH REPAIRS', 'UNION', 'KYLE', (SELECT tech_id FROM technician WHERE technician_name = 'KYLE'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('JKL3456', 10004, 'REPAIRED', 'AWAITING CHS REPAIRS', 'IREDELL', 'TRISTIN', (SELECT tech_id FROM technician WHERE technician_name = 'TRISTIN'));

-- PARTIAL units
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('MNO7890', 10005, 'PARTIAL', 'BER', 'RICHLAND 1', 'ANTHONY', (SELECT tech_id FROM technician WHERE technician_name = 'ANTHONY'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('PQR1122', 10006, 'PARTIAL', 'BUG UNIT', 'ANDERSON 5', 'ASHE', (SELECT tech_id FROM technician WHERE technician_name = 'ASHE'));

-- IMAGING COMPLETE units
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('STU3344', 10007, 'IMAGING COMPLETE', 'AWAITING REIMAGING', 'FT. MILL', 'CODY', (SELECT tech_id FROM technician WHERE technician_name = 'CODY'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('VWX5566', 10008, 'IMAGING COMPLETE', 'AWAITING REIMAGING', 'UNION', 'DARIUS', (SELECT tech_id FROM technician WHERE technician_name = 'DARIUS'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('YZA7788', 10009, 'IMAGING COMPLETE', 'AWAITING REIMAGING', 'IREDELL', 'CHRISTIAN', (SELECT tech_id FROM technician WHERE technician_name = 'CHRISTIAN'));

-- BER units
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('BCD9900', 10010, 'BER', 'BER', 'RICHLAND 1', 'GEOFF', (SELECT tech_id FROM technician WHERE technician_name = 'GEOFF'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('EFG1122', 10011, 'BER', 'BER', 'ANDERSON 5', 'JESS', (SELECT tech_id FROM technician WHERE technician_name = 'JESS'));

-- COMPLETED QA units
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('HIJ3344', 10012, 'COMPLETED QA', NULL, 'FT. MILL', 'TURNER', (SELECT tech_id FROM technician WHERE technician_name = 'TURNER'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('KLM5566', 10013, 'COMPLETED QA', NULL, 'UNION', 'ZAC', (SELECT tech_id FROM technician WHERE technician_name = 'ZAC'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('NOP7788', 10014, 'COMPLETED QA', NULL, 'IREDELL', 'TYREK', (SELECT tech_id FROM technician WHERE technician_name = 'TYREK'));

-- COMPLETED BUGS units
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('QRS9900', 10015, 'COMPLETED BUGS', NULL, 'RICHLAND 1', 'ANDRE', (SELECT tech_id FROM technician WHERE technician_name = 'ANDRE'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('TUV1122', 10016, 'COMPLETED BUGS', NULL, 'ANDERSON 5', 'TRINITY', (SELECT tech_id FROM technician WHERE technician_name = 'TRINITY'));

-- COMPLETED BER units
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('WXY3344', 10017, 'COMPLETED BER', NULL, 'FT. MILL', 'DAVID', (SELECT tech_id FROM technician WHERE technician_name = 'DAVID'));

-- Units with AWAITING DELL DEPOT
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('ZAB5566', 10018, 'DIAGNOSED', 'AWAITING DELL DEPOT', 'UNION', 'CRUZ', (SELECT tech_id FROM technician WHERE technician_name = 'CRUZ'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('CDE7788', 10019, 'REPAIRED', 'AWAITING DELL DEPOT', 'IREDELL', 'PAM', (SELECT tech_id FROM technician WHERE technician_name = 'PAM'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('FGH9900', 10020, 'PARTIAL', 'AWAITING DELL DEPOT', 'RICHLAND 1', 'RYAN', (SELECT tech_id FROM technician WHERE technician_name = 'RYAN'));

-- Units with AWAITING OOS PARTS
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('IJK1234', 10021, 'DIAGNOSED', 'AWAITING OOS PARTS', 'ANDERSON 5', 'GEOFF', (SELECT tech_id FROM technician WHERE technician_name = 'GEOFF'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('LMN5678', 10022, 'REPAIRED', 'AWAITING OOS PARTS', 'FT. MILL', 'BRANDON', (SELECT tech_id FROM technician WHERE technician_name = 'BRANDON'));

-- Units with DOA PART
INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('OPQ9012', 10023, 'PARTIAL', 'DOA PART', 'UNION', 'JOSH', (SELECT tech_id FROM technician WHERE technician_name = 'JOSH'));

INSERT INTO unit (service_tag, job_number, current_status, on_hold_status, customer_name, assigned_technician, current_tech_id)
VALUES ('RST3456', 10024, 'DIAGNOSED', 'DOA PART', 'IREDELL', 'KYLE', (SELECT tech_id FROM technician WHERE technician_name = 'KYLE'));

-- 6. Insert sample work history
INSERT INTO work_history (service_tag, job_number, technician_name, work_date, status_before, status_after, on_hold_status, notes)
VALUES ('FFH8VW3', 82071, 'GEOFF', '2025-01-15', NULL, 'DIAGNOSED', 'AWAITING WARRANTY / NON-ADP REPAIRS', 'Diagnosed the unit, awaiting parts.');

INSERT INTO work_history (service_tag, job_number, technician_name, work_date, status_before, status_after, on_hold_status, notes)
VALUES ('a1b2c3d', 12345, 'BRANDON', '2025-01-20', 'DIAGNOSED', 'REPAIRED', 'AWAITING QA', 'Repaired motherboard.');

INSERT INTO work_history (service_tag, job_number, technician_name, work_date, status_before, status_after, on_hold_status, notes)
VALUES ('STU3344', 10007, 'CODY', '2025-01-18', 'REPAIRED', 'IMAGING COMPLETE', 'AWAITING REIMAGING', 'Imaging completed successfully.');

INSERT INTO work_history (service_tag, job_number, technician_name, work_date, status_before, status_after, on_hold_status, notes)
VALUES ('HIJ3344', 10012, 'TURNER', '2025-01-22', 'REPAIRED', 'COMPLETED QA', NULL, 'Passed QA inspection.');

-- Verify data
SELECT 'Technicians' as label, COUNT(*) as count FROM technician
UNION ALL
SELECT 'Units', COUNT(*) FROM unit
UNION ALL
SELECT 'Work History', COUNT(*) FROM work_history;